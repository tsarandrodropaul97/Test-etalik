{% extends 'base.html.twig' %}

{% block title %}Gestion des attributs
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1 class="mb-4">Liste des Attributs</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class=" btn btn-primary">Enregistrer</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
